<mvc:View controllerName="horvath.staffingapp.controller.Table" 
    height="100%"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:t="sap.ui.table">
     <t:TreeTable id="tblCapacity" firstVisibleRowChanged="onScroll" rowSelectionChange="onRowSelectionChange" rowsUpdated="onRowsUpdated" columnResize="onColumnResize" toggleOpenState="onExpandRow" 
    sort="onSort" rootLevel="0" busy="{/busy}" busyIndicatorDelay="{app>/busyIndicatorDelay}" 
    selectionMode="{= ${app>/IsEditMode} ? 'MultiToggle' : 'None' }" showNoData="true" fixedColumnCount="9" 
    minAutoRowCount="5" visibleRowCountMode="Auto" rowHeight="30px" enableColumnReordering="false" 
    showOverlay="{= ${app>/IsFilterChanged} &amp;&amp; ${app>/IsFilterBarOpen}}">
        <t:rowSettingsTemplate>
            <t:RowSettings highlight="{rowHighlight}"/>
        </t:rowSettingsTemplate>
        <t:extension>
            <!-- <OverflowToolbar id="idCapacityToolbar" height="auto"> -->
                <!-- <Title id='idGridTitle\' text="{ path : \'/resourcesCount\', formatter: \'.formatTitle\' }"/>
				<Button id="idFilterToggleButton" icon="sap-icon://filter" tooltip="{i18n>TOGGLE_FILTER_TOOLTIP}" press="onToggleFilter" type="Transparent" enabled="{app>/IsDisplayMode}"/>
				<ToolbarSpacer/>
				<Button id="idBtnSaveSelect" text="{i18n>SAVE_SEL}" visible="{app>/IsEditMode}" enabled="{/rowsSelected}" press="onSaveSelected" type="Transparent"/>
				<Button id="idBtnRevert" text="{i18n>REVERT_EDITED_CELLS}" visible="{app>/IsEditMode}" enabled="{/rowsSelected}" press="onRevertSelected" type="Transparent"/>
				<Button id="idBtnEdit" text="{i18n>EDIT_ASSIGNMENTS_BUTTON}" visible="{app>/IsDisplayMode}" enabled="{= ${/resourcesCount} > 0}" press="onEdit" type="Emphasized"/>
				<Button id="idBtnPersonalization" icon="sap-icon://action-settings" press="onOpenPersoDialog" type="Transparent"/>
				<Button id="idBtnExcel" icon="sap-icon://excel-attachment" tooltip="{i18n>EXPORT_TO_SPREADSHEET}" type="Transparent" press="onExportToExcel"/> -->
            <!-- </OverflowToolbar> -->
            <!-- <OverflowToolbar id="idInfoToolbar" active="true" visible="{= ${app>/InfoBarFilterCount} > 0 &amp;&amp; !${app>/IsFilterBarOpen}}" design="Info" press="onInfoBar"> -->
                <!-- <Text id="idFilterInfoText" text="{parts: [\'app>/InfoBarFilterCount\', \'app>/InfoBarItems\'],formatter: \'.formatInfoBarText\'}"/> -->
            <!-- </OverflowToolbar> -->
        </t:extension>
    </t:TreeTable>
</mvc:View>