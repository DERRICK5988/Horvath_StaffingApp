<mvc:View controllerName="horvath.staffingapp.controller.App" displayBlock="true"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc" width="100%" height="100%">
    <Shell>
        <App id="app" busy="{appView>/busy}" busyIndicatorDelay="{appView>/delay}">
        </App>
    </Shell>
</mvc:View>